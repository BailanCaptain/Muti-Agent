# V16.3-clean Audit Report v2 — 深审版

> **vs v1**：v1 浅扫了 feature 顶部 spec + git status，没看实际代码 / schema / 接口质量 / 跟 V16.3 愿景的契合度。**小孙批评："已有实现可能是垃圾就是没用的就是不适合的，复用没意义"**。v2 深审 8 个 feature 完整 spec + 关键代码 + 跟 V16.3 愿景对账，每条复用判断有代码引用。
>
> **核心结论**：v1 高估了"复用"的边际收益（22 天 → 实际 15-18 天），并且发现 F002/F019 跟 V16.3 章节 11/13 **完全不是一回事**——决策 3 的前提错了。

---

## 1. 8 个 feature 深审结果

### 1.1 F018 Context Resume Rebuild — ✅ 真生效（外部证物）

**spec 状态**：done（2026-04-18 close，5 Phase merged，14 轮 Codex review）

**实际质量**（实读代码后判断）：
- `packages/api/src/orchestrator/session-bootstrap.ts` 197 行：7 区段（identity / threadMemory / digest / task / recall / tools / guard）+ 闭合标签 + token 硬顶 + drop order + sanitize
- `packages/api/src/orchestrator/sanitize-handoff.ts` 40 行：4 层防御（ASCII control / invisible format / wrapper 闭合标签 / 关键词整行）+ surrogate 边界 + bidi marks 加固
- `packages/api/src/orchestrator/auto-resume.ts` 76 行：B015 fix（complete 短路）+ Bootstrap 风格续接消息
- B019 修复后真生效证物：`SELECT COUNT(*) FROM message_embeddings = 3` / recall top score=0.3662 / `model-not-ready` warn 0 条

**vs V16.3 复用判断**：
| V16.3 章节 | 复用程度 | 证据 |
|---|---|---|
| 章节 4 注入合约 | ✅ **复用 SessionBootstrap 7 区段框架** | 在 7 区段基础上加 viewfinder / capability / handbook 切片 / memory_preflight 等新区段 |
| 章节 7 raw drop taint | ⚠ **借鉴 sanitizeHandoffBody 设计模式** | raw drop 是新场景（用户拖文件），需新写 sanitize-raw-drop.ts；但参照 sanitize-handoff.ts 的多轮加固范式 |
| 章节 8 RoomCompiler | ⚠ **部分复用 TranscriptWriter** | TranscriptWriter 写 JSONL 冷存储 + 稀疏索引；V16.3 章节 8 RoomCompiler 是更广泛的 wiki_events 框架（含 cursor + 二阶段提交 + sealed_at），需在 TranscriptWriter 基础上扩展 |
| 章节 12 Adaptive Recall | ✅ **直接复用 recall_similar_context MCP** | 已 production，需扩展 5 级 fallback + Hard Gate |
| 章节 26 Phase 1 Pre-compile | ✅ **直接复用 embedding-service.vectorSearch** | B019 修复后真生效 |

**verdict**：✅ **重度复用** — F018 是 V16.3 最大的基础。**节省工时 8 天**（vs v1 估 12 天，v2 下调因 SessionBootstrap 需要扩展，不是直接 copy）

---

### 1.2 F004 Context Memory Authoritative — ✅ 核心不变量已落地

**spec 状态**：done（2026-04-11 close，1 day 实施）

**实际质量**：
- `packages/api/src/orchestrator/context-assembler.ts` 200+ 行：`assemblePrompt(input, memoryService)` 单一入口，17 参数 struct，{systemPrompt, content} 两段式返回
- 系统 prompt 部分：base identity + rolling summary（含 sanitize + 8K 截断）+ sopBookmark
- content 部分：F018 SessionBootstrap 注入（line 118-137，仅 nativeSessionId === null 时）+ task header + Phase 1 header + preamble
- F018 AC5.3/5.4 已废弃 `--- 你之前的发言 ---` / `--- 近期对话 ---` 原对话重灌

**vs V16.3 复用判断**：
| V16.3 章节 | 复用程度 | 证据 |
|---|---|---|
| 章节 4 assembleSystemPrompt | ✅ **扩展现有 `assemblePrompt`，不新建** | F004 已是单一入口，V16.3 加新参数（roomId / agentAlias / scenario / handoffContext）和新区段（viewfinder / capability / handbook 切片）即可。**避免函数分裂！** |
| 章节 8 RoomCompiler | ✅ **复用历史注入路径** | roomSnapshot 已从 DB 读真实消息（F004 不变量），V16.3 章节 8 在此加 cursor + checkpoint |

**verdict**：✅ **直接复用扩展** — **节省工时 4 天**

**新决策点 6**：V16.3 章节 4 的函数命名 `assembleSystemPrompt` 跟 F004 现有 `assemblePrompt` 不一致，立项前必须统一（推荐扩展现有函数）。

---

### 1.3 F011 Backend Hardening Drizzle — ⚠ schema 风格沿用，但表语义不复用

**spec 状态**：done（2026-04-15 close，5 commits / 21 files / +2800/-269 行 / 513 tests）

**实际质量**：
- `packages/api/src/db/schema.ts` 9 张核心表：session_groups / threads / message_embeddings / messages / invocations / agent_events / session_memories / tasks / authorization_rules / authorization_audit + WorkflowSop 相关表
- 外键约束：messages.thread_id / invocations.thread_id / agent_events.invocation_id
- `runTx()` 事务包裹 + ensurePreMigrationBackup + rollbackDatabase

**vs V16.3 复用判断**：
| V16.3 章节 | 复用程度 | 证据 |
|---|---|---|
| 章节 5 wiki_events 表 | ❌ **不复用 agent_events** | agent_events 是 agent 工具调用 / runtime 事件；V16.3 wiki_events 是 wiki 操作事件（ingest / promote / merge / supersede）。**语义完全不同，新建表** |
| 章节 14 6 类记忆桶 | ❌ **不复用 session_memories** | session_memories 是单一 rolling summary 文本（无 type 字段）；V16.3 6 类记忆桶要求每条 memory 有 frontmatter（canonical_owner_path / type / sources / cross_refs / dedup_decision）。**schema 不兼容，新建表** |
| 全局 schema 风格 | ✅ **沿用 drizzle-orm + better-sqlite3 + 外键 + 事务范式** | V16.3 加新表使用相同 framework |
| 工具函数 | ✅ **复用 ensurePreMigrationBackup / rollbackDatabase / runTx** | 不重写 |

**verdict**：⚠ **范式沿用，表语义不复用** — **节省工时仅 1-2 天**（vs v1 估 2 天，v2 下调因新表多）

**新决策点 7**：6 类记忆桶 schema 设计：
- A：1 张多 type 表（type enum: room/project/user/feedback/conversation/work），同一表根据 type 路由 — **推荐**
- B：6 张表（每类一张）— 重复字段太多
- C：复用 session_memories 加 type 字段 — schema 太脏

**新决策点 8**：wiki_events 是否新建：
- A：新建 wiki_events 表（独立 event source）— **推荐**（与 agent_events 语义完全不同）
- B：在 agent_events 加 wiki_event 子类型 — 不推荐（混合两种事件流）

---

### 1.4 F002 Decision Board — ❌ 跟 V16.3 章节 11 完全不同维度

**spec 状态**：done（2026-04-11 close，280/283 tests）

**实际质量**：
- DecisionBoard 状态机：pending decision 集合 + add / dedupe（SHA-1 hash）/ withdraw / flush
- SettlementDetector：3-AND settle 条件（无 running ParallelGroup + 空 dispatch queue + 无 running turn）+ 2000ms debounce
- ChainStarterResolver：单点 dispatch 收敛（hopIndex=0 最早 assistant）
- 前端 DecisionBoardModal 批量面板（深墨蓝 + 金线 + serif 编号）

**vs V16.3 章节 11 Viewfinder anti-drift 对比**：

| 维度 | F002 | V16.3 章节 11 |
|---|---|---|
| 解决问题 | 多 agent 拍板请求 thread 分叉 | viewfinder（agent 看到的上下文摘要）漂移 |
| 数据特性 | 临时状态，settle 后 flush 清空 | append-only 永久流水账（room_decisions ledger）|
| 时间窗口 | session 内（1 次讨论）| 跨 session（项目生命周期）|
| 触发 | inline `[拍板]` 解析 | 决策被引用 / 被推翻 / 被遗忘的 telephone game |
| 防护机制 | sha-1 hash dedupe | tombstone + monthly recompile + Decision Coverage Check |

**verdict**：❌ **完全不同维度，不复用、不合并、不 supersede** — **F002 独立保留，V16.3 章节 11 独立设计**

**重要**：v1 audit 错误推荐"F002 数据作为 V16.3 ledger 底座"——撤回。它们不是同一个东西。决策 3 的前提错了。

---

### 1.5 F019 Skill Bulletin Board — ❌ 跟 V16.3 章节 13 完全不同维度

**spec 状态**：done（2026-04-17 close，P1-P4 + acceptance-guardian PASS）

**实际质量**：
- WorkflowSopService 状态机：stage 枚举（kickoff / impl / quality_gate / review / merge / completion）+ batonHolder + resumeCapsule + checks
- system prompt 注入 `sopStageHint` 一行（per invocation，feature 绑定时）
- MCP tool `update_workflow_sop` + HTTP callback 双路径
- BOOTSTRAP.md（15 个 skill 压缩选择表 + 三 CLI 加载方式说明）
- `scripts/sync-skill-mounts.sh` 三挂载点同步

**vs V16.3 章节 13 alias-aware capability registry 对比**：

| 维度 | F019 | V16.3 章节 13 |
|---|---|---|
| 视角 | feature → stage → skill | agent → 6 槽位能力 |
| 对象 | feature 当前 SOP 阶段 | agent 自身能力声明 |
| 数据 | WorkflowSop 状态机 | agent-capabilities.yaml |
| 注入 | sopStageHint（一行）| capability_digest（150-250 tok 6 槽）|

**verdict**：❌ **完全不同维度，不复用** — **F019 独立保留，V16.3 章节 13 独立设计**

---

### 1.6 F022 Left Sidebar Redesign — ⚠ ROOM ID namespace 父级

**spec 状态**：done（2026-04-21 close，Phase 1-3.5）

**实际质量**：
- session_groups.room_id 全局递增（R-001, R-002, ...）+ 唯一索引
- Haiku 自动命名（首条 assistant 后 debounce 2.5s）+ F/B/D/Q 前缀分类
- session_groups 加 archived_at / deleted_at / titleLockedAt 字段
- 软删 + 归档列表 + 右键菜单四件套（重命名 / 清除项目标签 / 归档 / 软删）
- HaikuRunner 子进程 stdin 关闭 + 15s 超时（修复 AC-14f 回归）

**vs V16.3 复用判断**：
| V16.3 章节 | 复用程度 | 证据 |
|---|---|---|
| 章节 9 agent-sessions ledger | ✅ **R-XXX namespace 父级** | V16.3 S-XXXX agent-sessions ledger 文件路径含 R-XXX 作 namespace（`agent-sessions/R-042/S-001-黄仁勋.md`）|
| 软删 / 归档基础 | ✅ **复用 session_groups archived_at / deleted_at** | V16.3 不重做软删机制 |

**verdict**：⚠ **ROOM ID 联动 + 复用软删基础** — **节省工时 1 天**

---

### 1.7 F023 MCP Unified Mounting — ✅ V16.3 update_wiki 直接挂

**spec 状态**：done（2026-04-20 close，Phase A + Phase B 全闭环）

**实际质量**：
- 三家 CLI 项目级 MCP 配置：`.mcp.json`（Claude）/ `.codex/config.toml`（Codex）/ `.gemini/settings.json`（Gemini）
- runtime spawn 时 env: {MULTI_AGENT_API_URL, MULTI_AGENT_INVOCATION_ID, MULTI_AGENT_CALLBACK_TOKEN} 进程继承链
- `packages/api/src/mcp/server.ts` 947 行，标准 JSON-RPC 2.0
- `tools/list` 和 `tools/call` 两个核心 method（line 898-902）
- MCP server 路径修复（runtime spawn 传 cwd → projectRoot）

**vs V16.3 章节 6 update_wiki MCP 复用判断**：
- ✅ **直接挂载** — V16.3 update_wiki 加新 tool 注册到 server.ts（list 加 metadata + call 加 case），三家 CLI 自动可用
- ✅ 无需新设挂载机制
- 无需重写 `getCallbackIdentity()` / `requestJson()` / JSON-RPC 处理逻辑

**verdict**：✅ **直接复用挂载机制** — **节省工时 3 天**

---

### 1.8 B022 Prompt Injection 修复 — ⚠ 借鉴模式

**bug 状态**：done（2026-04-23 修复，commit aa55f82）

**实际修复**：
- 砍 CLAUDE.md / AGENTS.md / GEMINI.md 到最小集（仅 Iron Laws + 身份 + 双指针）
- 删 L0_DIGEST 常量（29 行硬编码）
- `loadSharedRules()` fail-closed（找不到抛错，不再静默 fallback）
- shared-rules.md = 项目家规**单一真相源**

**vs V16.3 复用判断**：
| V16.3 章节 | 复用程度 | 证据 |
|---|---|---|
| 章节 7 raw drop taint | ⚠ **借鉴 fail-closed + 单一真相源原则** | raw drop sanitize 失败时 fail-closed（不静默通过）|
| 章节 27 handbook | ⚠ **借鉴单一真相源契约** | handbook fail-closed + 切片即时读 |

**verdict**：⚠ **借鉴模式，不直接复用代码** — **节省 0 天，但避免重蹈覆辙**

---

## 2. 现有项目状态对 V16.3 决策的影响

### 2.1 决策 1：shared-rules.md vs handbook（小孙澄清后）

**v2 修订**：你说得对，**职责完全不同**：
- shared-rules.md = **项目级团队协作家规**（铁律 / 第一性原理 / @-rule / Skill 路由）
- V16.3 handbook = **wiki 操作级手册**（编译规则 / Sanitize 规则 / Agent 动作手册 / Dev human）

B022 已确立 shared-rules.md 是项目家规单一真相源（fail-closed + 删 L0_DIGEST）。V16.3 handbook 是 wiki 操作单一真相源。**两者并存不冲突**，handbook 不替代 shared-rules.md。

**Action**：V16.3 章节 27 加一段"职责边界"明示：handbook 不管项目家规，shared-rules.md 不管 wiki 操作。

### 2.2 决策 2：F012 时机（小孙澄清）

F012 已 done，文档 status 未更新（实际开发已闭环）。

**Action**：V16.4 立项时顺带更新 F012 frontmatter status: in-progress → done + 加 close timeline。

### 2.3 决策 3：F002 / F019 / F020 vs V16.3 章节 11（v1 错前提）

**v1 错前提**：误以为 F002/F019/F020 跟 V16.3 章节 11 是同一类东西。**深审后撤回**：
- F002 = 实时讨论收敛（session 内）
- F019 = feature SOP 阶段（feature 视角）
- F020 = 决策卡挂载矩阵（spec 未做，跟 F002 相关）
- V16.3 章节 11 = 记忆架构防漂移 ledger（跨 session 永久流水账）

**完全不同维度，不存在合并 / supersede 关系**。

**Action**：F002 / F019 / F020 跟 V16.3 章节 11 都独立保留。V16.3 立项时 audit F020 spec 是否还要做（独立判断，跟 V16.3 无关）。

### 2.4 决策 4：F015 / F017 / F026 spec（小孙暂缓）

待立项前 audit。F026 先合 dev 再做 V16.3。

### 2.5 决策 5：LLM 模型选型（小孙拍 升级）

| 角色 | 模型 |
|---|---|
| compile-LLM | **Opus 4.7** |
| sanitize-LLM | **Sonnet 4.6** |
| room-compiler-LLM | **Opus 4.7** |

---

## 3. 新发现的真决策点（深审后）

### 决策 6：assemblePrompt 函数命名（🔴 高优）

V16.3 章节 4 设计了 `assembleSystemPrompt`，F004 现有 `assemblePrompt`。

- A（推荐）：扩展现有 `assemblePrompt`，加新参数（viewfinder / capability_digest / handbook 切片 / memory_preflight）和新区段 — **避免函数分裂**
- B：新建 `assembleSystemPrompt`，跟 `assemblePrompt` 并存 — 维护两套，必 drift

### 决策 7：6 类记忆桶 schema（🟡 中优）

- **A（推荐）**：1 张多 type 表（type enum: room/project/user/feedback/conversation/work），同一表根据 type 路由
- B：6 张表（每类一张）— 重复字段太多
- C：复用 session_memories 加 type 字段 — schema 太脏（session_memories 当前是 rolling summary 文本，没 frontmatter 概念）

### 决策 8：wiki_events 表新建 vs 复用 agent_events（🟡 中优）

- **A（推荐）**：新建 wiki_events 表（独立 event source）— agent_events 是 runtime 事件，wiki_events 是 wiki 操作事件，语义完全不同
- B：在 agent_events 加 wiki_event 子类型 — 混合两种事件流，查询低效

---

## 4. 工时调整 v2

### 原 V16.3 章节 21 估算
- 单人串行：80-100 天
- 多 agent 并行：11-17 周（2.5-4 个月）

### Audit v1 估算（浅扫）
- 节省 -22 天
- 单人串行：60-80 天

### Audit v2 估算（深审）
| 复用源 | v1 估 | v2 实测 | 调整原因 |
|---|---|---|---|
| F018 SessionBootstrap + sanitize + embedding | -12 天 | **-8 天** | SessionBootstrap 需要扩展加新区段，不是直接复用 |
| F004 assemblePrompt 框架 | -4 天 | **-4 天** | 直接扩展现有函数 |
| F011 schema 风格 + 工具函数 | -2 天 | **-1 天** | 新表多（wiki_events / 6 类记忆桶 / room_decisions / agent-sessions ledger / draft 表 等）|
| F023 MCP 直接挂 | -3 天 | **-3 天** | 加新 tool 即可 |
| F022 ROOM ID 联动 + 软删基础 | -1 天 | **-1 天** | 直接联动 |
| F002 / F019 / B022 | 0 天 | **0 天** | 独立保留 / 借鉴模式 |
| **总节省** | **-22 天** | **-17 天** |

**调整后估算**：
- 单人串行：**63-83 天**
- 多 agent 并行：**9-14 周（2.25-3.5 个月）**

---

## 5. 立项前 final TODO list

### 🔴 必须拍板（高优）
1. ✅ 决策 1（shared-rules vs handbook）— 你已澄清，不冲突
2. ✅ 决策 2（F012 时机）— 你已澄清，F012 done
3. ✅ 决策 5（LLM 模型选型）— 你已拍升级
4. ❌ 决策 3（F002/F019/F020 vs V16.3 章节 11）— v1 错前提，撤回，**不需要拍**
5. ⏸ 决策 4（F015/F017/F026）— 暂缓，立项前 audit
6. **决策 6（assemblePrompt 函数命名）— 待拍**
7. **决策 7（6 类记忆桶 schema）— 待拍**
8. **决策 8（wiki_events 是否新建）— 待拍**

### 🟡 V16.4 plan 修订（基于深审）
- 章节 4 改成"扩展现有 assemblePrompt"（决策 6）
- 章节 5 加 wiki_events 表新建（vs agent_events 区分）（决策 8）
- 章节 14 6 类记忆桶 schema 用 1 表多 type（决策 7）
- 章节 26 加 LLM 模型选型表（决策 5）
- 章节 27 加"shared-rules.md 职责边界"明示（决策 1）
- 章节 21 Phase 拆分按"扩展 F018 SessionBootstrap" / "扩展 F004 assemblePrompt" 重写依赖关系
- F012 frontmatter status 顺带更新

### 🟢 立项启动（按 V16.4）
- F0XX-A 后端基础设施（P0-P5 + P3.5 + P3.6 + P7.5）— 含 K Handbook H2 切片
- F0XX-L LLM 编译 3 阶段
- F0XX-M IngestModal UX
- F0XX-N (新) capability registry 6 槽位
- F0XX-O (新) Viewfinder anti-drift ledger

---

## 6. v2 vs v1 关键差异

| 维度 | v1（浅扫）| v2（深审）|
|---|---|---|
| 复用工时节省 | -22 天 | **-17 天** |
| F002 vs V16.3 章节 11 判断 | 错（推荐合并）| **正（完全不同维度，独立保留）** |
| F019 vs V16.3 章节 13 判断 | 错（推荐合并）| **正（完全不同维度，独立保留）** |
| 决策点数 | 5 | **3 个真决策（6/7/8）+ 2 个已澄清（1/2/5）+ 1 个错前提撤回（3）+ 1 个暂缓（4）** |
| 代码引用 | 无 | **每条复用判断有代码 line 引用** |

---

**生成于**：2026-05-07，黄仁勋
**深审依据**：F004 / F011 / F018 / F019 / F022 / F023 / F002 spec + 关键代码（context-assembler.ts / session-bootstrap.ts / sanitize-handoff.ts / auto-resume.ts / schema.ts / mcp/server.ts）+ B022 修复细节
**工时**：约 6 小时（小孙拍 "全部深度审视，时间不在乎，质量优先"）
