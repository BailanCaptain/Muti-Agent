# V16.4 Audit Report v4 — F026 合 dev 后的 V16.4 重评

> **触发**：F026 A2A 可靠通信层 v2 合 dev (944b7b1, 2026-05-08, 149 commits squash, DoD-1/2/3 全绿)。  
> v3 报告写于 2026-05-07 时 F026 还在 worktree，决策 4 标 "deferred 立项第一批后做"。  
> 现 F026 落地 → 必须重评 V16.4 plan 是否冲突 / 复用 / 修订。
>
> **小孙约束**："skill 不要动先" — 本次 audit **只动 V16.4 plan**，不改 feat-lifecycle / quality-gate / shared-rules.md / iron law hook 等任何 skill。
>
> **核心结论**：V16.4 plan 与 F026 落地物 **0 schema 冲突 / 0 过时引用**。仅 6 处对接面需要在 plan 加 reference 注解，工时基线不变（63-83 单人天 / 9-14 周并行）。建议出 **V16.5 增量修订**（不重写）。

---

## 1. F026 落地全景（基于 944b7b1 实读代码）

### 1.1 新增 schema

| 表 / 字段 | 文件 | 说明 |
|---|---|---|
| **`a2a_calls`** 表（13 字段 + 4 索引）| `packages/api/src/db/schema.ts:218-244` | Call Tree 协议真相源（callId / parentCallId / rootCallId / issuerId / convenerId / onBehalfOf / replyTo / deadlineAt / joinSetId / status / envelopeVersion / sessionGroupId）|
| **`a2a_worklists`** 表（9 字段 + 2 索引）| `packages/api/src/db/sqlite.ts` | Worklist 树形收敛（同构 a2a_calls.parent_call_id），settle 不变量：items 全 done **且** 子 worklist 全 settled |
| `messages.a2a_call_id`（新字段）| schema.ts:84 | a2a 派发产生的 connector message 关联到 a2a_calls |
| `messages.retry_count` / `retry_reasons`（新字段）| schema.ts:78-80 | P3.1 派发协议 retry 兜底 |
| `agent_events.invocation_id` 改 nullable | schema.ts:113-118 | DiscussionCoordinator / mention-router gray-zone 系统级事件用 NULL |

### 1.2 新增运行时模块

| 模块 | 文件 | 职责 |
|---|---|---|
| **CallRegistry** | `orchestrator/call-registry.ts` | 操作 a2a_calls 表 + CAS（kill -9 重启可恢复，I4） |
| **WorklistRegistry** | `orchestrator/worklist-registry.ts:75` (241 行) | 操作 a2a_worklists + 树形 settle |
| **A2AChainRegistry** | `orchestrator/a2a-chain.ts:27` | in-memory 调用链（render-only senderAlias / triggerMessageId） |
| **InvocationRegistry** | `orchestrator/invocation-registry.ts:25` | in-memory active invocations（即"RunningSlots"实质） |
| **EnvelopeBuilder** | `orchestrator/envelope-builder.ts` (116 行) | 协议层 / 任务层 + display_mode 三态 |
| **mention-router** 三层（layer1/2/3） | `orchestrator/mention-router.ts:614` (重写) | fail-closed + AST + role 守卫 + on-behalf 反推 |
| **WorklistAdvance / WorklistContinuation** | `orchestrator/worklist-advance.ts` / `worklist-continuation.ts` | drain-based settle |
| **A2ALifecycle** service | `services/a2a-lifecycle.ts` (126 行) | call lifecycle 服务化 |
| **DispatchRetryCoordinator / DispatchRetryEvent** | `services/dispatch-retry-*.ts` | retry-guard P3.1 |
| **MentionGrayZoneEvent** | `services/mention-gray-zone-event.ts` (113 行) | gray-zone 事件流 |
| **GET /debug/a2a** 路由 | `routes/debug-a2a.ts` (79 行) | **只读**调试视图（4 种查询：root / parent / status / session+view=tree）|

### 1.3 整删（不再可引用）

| 删除物 | 原文件 | 说明 |
|---|---|---|
| **ParallelGroupRegistry** | `orchestrator/parallel-group.ts` (-318 行) | clean-cut Step 3 |
| **parallel_think MCP tool** | mcp server 端 | 整删 |
| **DiscussionCoordinator** | runtime / dispatch | 整删 |
| **phase1-header / phase2-header / collapsible-group** | orchestrator | 整删 |
| **return-path 旧路径** | `orchestrator/return-path.ts` (-63 行) | 单轨直切 |
| **`parallel_groups` 表**（drizzle schema）| schema.ts | 老 DB 死表残留，drizzle 不读 |

合计 -5300 行 / 33 文件整删。

---

## 2. V16.4 plan vs F026 落地：扫描结论

### 2.1 过时引用清查（grep V16.4-final.md 全文）

| 关键字 | 命中数 | 说明 |
|---|---|---|
| `parallel_think` | **0** | ✅ V16.4 plan 没引用 |
| `DiscussionCoordinator` | **0** | ✅ |
| `parallel_group` / `parallel-group` | **0** | ✅ |
| `phase1-header` / `phase2-header` / `collapsible-group` | **0** | ✅ |
| `return-path` / `return_path` | **0** | ✅ |
| `RunningSlots` / `A2AChainRegistry` / `InvocationRegistry` / `CallRegistry` | **0** | ⚠️ 不是冲突，是缺对接 reference |
| `/debug/a2a` / `debug-a2a` | **0** | ⚠️ 同上，应明示复用 |

**结论**：V16.4 plan 写于 F026 落地前，**完全是"未来设计"，没引用任何现已过时的 F026 名词**——非常干净，0 处需要"删过时引用"。

### 2.2 schema 冲突扫描

| F026 新增 | V16.4 新增 | 冲突？ |
|---|---|---|
| `a2a_calls`（call lifecycle）| `wiki_events`（wiki 操作事件）| ❌ 命名空间正交 |
| `a2a_worklists`（worklist 树形）| `room_decisions`（决策 ledger）| ❌ 维度不同 |
| `messages.a2a_call_id`（协议关联）| `messages` 不动 | ❌ |
| `agent_events.invocation_id` nullable | `wiki_events` 独立表 | ❌ V16.4 章节 5 已明示"新建 wiki_events 不复用 agent_events" |
| 无 `room_agent_sessions` | `room_agent_sessions`（agent session ledger）| ❌ V16.4 独有 |
| 无 `wiki_memories` | `wiki_memories`（6 类记忆）| ❌ V16.4 独有 |

**结论**：**0 schema 冲突**。F026 关注 A2A 协议状态，V16.4 关注记忆架构，命名空间完全正交。

---

## 3. 6 个对接点详查（V16.4 plan 应该 reference F026 落地物）

### 对接点 ① — 章节 4 a2a_handoff scenario（line 296）

**V16.4 现状**：
```ts
scenario?: 'session_bootstrap' | 'wake_up' | 'a2a_handoff'
handoffContext?: { receiverAlias, taskSummary } | null
```

**F026 落地**：Envelope 双层（协议层含 onBehalfOf / parentCallId / displayMode / replyTo；任务层含 task payload）

**修订建议（M1）**：章节 4 加一段说明 — `handoffContext` 由 F026 EnvelopeBuilder 在派发时填充：
- `receiverAlias` ← `envelope.protocol.convenerId / onBehalfOf`
- `taskSummary` ← `envelope.task.body`（已经过 Envelope 中性改写）

**改动量**：plan 加 5-8 行说明，**不改函数签名**。

---

### 对接点 ② — 章节 9 room_agent_sessions.open_threads（line 906）

**V16.4 现状**：
```yaml
open_threads:
  - "还要派范第 4 轮 verify"
  - "viewfinder 6 段未拍板"
```

**F026 落地**：a2a_calls 表已记录所有 pending/working call（含 deadline_at）

**修订建议（M2）**：章节 9 加一行 — 当 open_thread 是"等回复"型时，可 reference `a2a_calls.callId`：
```yaml
open_threads:
  - text: "还要派范第 4 轮 verify"
    a2a_call_id: "c_xxxx"  # ← F026 a2a_calls.call_id（可空）
    status: "pending"      # ← 实时取自 a2a_calls.status
```

**改动量**：plan 加 8-10 行 schema 注解，**不改 room_agent_sessions 表**（a2a_call_id 只在 JSON open_threads 字段内 reference，不加列）。

---

### 对接点 ③ — 章节 11 viewfinder §3 §4 证据链（line 1135-1140）

**V16.4 现状**：
```
## 3. 下一步 + 谁做
派范第 4 轮 verify（黄仁勋 owner，基于 D-021 [decision_id=21, msg_470]）

## 4. 等谁 / blocker
等小孙拍前端 + 调度章节...
```

**F026 落地**：a2a_calls.status / a2a_calls.deadline_at 实时反映"等谁"

**修订建议（M3）**：章节 11 §3 §4 证据链可叠加 a2a_calls reference：
```
## 4. 等谁 / blocker
等范第 4 轮 verify [a2a_call=c_xxxx, status=pending, deadline 17:30]
```

**注意**：viewfinder 不持久化 a2a_calls 数据（避免双真相源），仅在编译时通过 `callRegistry.findByStatus('pending')` 实时查询注入。**德彪建议的"瘦身"原则保留** — 不新加第 7 段，只在现有 §4 加引用。

**改动量**：plan §4 加一行注解，编译时调用 callRegistry。

---

### 对接点 ④ — 章节 18 RuntimeLog（line 1665+）

**V16.4 现状**：5 个 tab（viewfinder / prompt-inspector / draft-approval / warnings / knowledge-base），无 a2a tab。

**F026 落地**：
- 后端 `GET /debug/a2a`（4 种查询）✅ 已有
- 前端组件：at-pill / origin-capsule / timeout-tombstone / foldable-group / display_mode dispatcher / connector-bubble ✅ 已有

**修订建议（M4）**：章节 18 加一段"V16.5 修订：A2A 状态复用 F026 已落地组件"：
- **不新加** a2a tab
- 取景器 tab 内**联动** F026 前端组件（at-pill / connector-bubble 显示在 viewfinder §4 等谁段）
- prompt-inspector tab 加一行"📞 A2A pending calls: 2"，点击跳 `/debug/a2a?session=<gid>&view=tree`

**改动量**：plan 加 10-15 行说明 + mockup 注释。**前端代码不重做**（复用 F026 落地组件）。

---

### 对接点 ⑤ — 章节 21 风险表"F026 接口未拍前实施 P5"（line 2091）

**V16.4 现状**：
```
| F026 接口未拍前实施 P5 | P5 实施前必须小孙拍 |
```

**F026 落地**：F026 接口已落地（CallRegistry / WorklistRegistry / EnvelopeBuilder / /debug/a2a 全部）

**修订建议（M5）**：章节 21 风险表**删除此行**（风险已消除）。

**改动量**：plan 删 1 行。

---

### 对接点 ⑥ — Changelog（章节 0 line 9）

**修订建议（M6）**：章节 0 加 V16.5 Changelog 条目：
```
### V16.5 (2026-05-08) — F026 合 dev 后增量修订
- M1 章节 4 a2a_handoff handoffContext 来源 F026 envelope 字段
- M2 章节 9 open_threads 可 reference a2a_calls.callId
- M3 章节 11 viewfinder §4 引用 a2a_calls.callId/status 作为证据链
- M4 章节 18 RuntimeLog 复用 F026 落地前端组件 + /debug/a2a 接口
- M5 章节 21 风险表删 "F026 接口未拍前实施 P5"（已消除）
```

**改动量**：plan 加 8-10 行 Changelog。

---

## 4. R-201 audit v3 C 类痛点 → F026 解决映射

audit v3 因 F026 deferred，C 类 6 个 A2A 痛点标 "V16.4 不解，等 F026"。F026 落地后映射如下：

| audit v3 痛点 | F026 落地解决 | 不变量 / 回归用例 |
|---|---|---|
| **C1** 跨 thread 互不可见（"德彪说他没看见仁勋的问题"）| **I8** A2A 对 agent 透明 + **I7** Envelope 双层（β/γ 双路径 + on-behalf 语义）| ADR-004 guard CI |
| **C2** @ 不生效（"为什么@又不生效了"）| **I1'** 三层 mention fail-closed + AST + role 守卫 | M3 R-190 @ 不生效 回归 |
| **C3** 误唤醒（"@桂芬也起来了"）| **I9** Markdown-AST + code/backtick 屏蔽 + **I1'** layer3 role 守卫 | mention-router.role-guard.test |
| **C4** A2A 失真截断（"信息A会被截断吗"）| **I7** Envelope 双层（任务层不被协议层截断）| M5 R-034 回程 payload 截断 回归 |
| **C5** A2A 收敛混乱（5 段长场景）| **I2-a** Worklist 树形续推 + **I2-b** Call lifecycle CAS | M4 R-188 不收敛 回归 |
| **C6** 串房（"R-022 排队跑到 R-021/R-023"）| **I3** Broadcaster 后端 sessionGroupId 强隔离 | fuzz 10000 条 0 泄漏 |

**结论**：R-201 audit v3 的 C 类 6 痛点 **F026 已全解**（11 不变量 + 14 症状回归覆盖）。V16.4 不需要再处理 A2A 业务层。

---

## 5. audit v3 决策 4 状态更新

audit v3 决策 4：F015 / F017 / F026 spec 状态 → 暂缓。

**v4 更新**：

| feature | v3 状态 | v4 状态 | 证据 |
|---|---|---|---|
| **F026** A2A 可靠通信层 | spec → deferred | ✅ **done** | 944b7b1 / 2026-05-08 / DoD-1/2/3 全绿 / API 1432/1440 PASS |
| **F015** 调度状态持久化 | spec | 🟢 **superseded by I4** | F026 commit msg "Supersedes F015 — 被 I4 吞并"（A2AChainRegistry / WorklistRegistry / RunningSlots 持久化 + CAS 已覆盖） |
| **F017** Cross-Room Awareness | spec | ⏸ **N/A** | F026 没改 cross-room；保持 deferred 立项后再 audit |

**决策 4 → done**（F015 / F026 闭环；F017 单独后续）。

---

## 6. 工时影响

### 6.1 重算

| 维度 | v3 估 | v4 估 | 调整 |
|---|---|---|---|
| F026 已落地节省 V16.4 工时 | 0 天 | **0 天** | V16.4 plan 本来就没设计 A2A 共用基础设施，F026 落地不影响 V16.4 开发量 |
| 新增对接 reference 注解（6 处 M1-M6）| - | **+0.5 天** | plan 改动量极小，主要是文档 |
| audit v3 决策 4 闭环（F015 不再立项）| - | **节省 5-8 天**（V16.5 不再为 F015 spec 立项工时）|
| **净影响** | | **-5-8 天**（节省 F015 立项工时）|

### 6.2 V16.5 工时基线

- v3：63-83 单人天 / 9-14 周并行
- **v4：58-78 单人天 / 8-13 周并行**（节省 F015 5-8 天）

---

## 7. V16.5 修订清单（skill 不动先）

按对接点编号：

| # | 章节 | 修订 | 改动量 |
|---|---|---|---|
| **M1** | 4（line 296）| handoffContext 来源 F026 envelope 字段说明 | +5-8 行 |
| **M2** | 9（line 906）| open_threads 加 a2a_call_id reference 注解 | +8-10 行 |
| **M3** | 11（§4 line 1138）| viewfinder §4 加 a2a_calls 证据链引用 | +3-5 行 |
| **M4** | 18（line 1665+）| RuntimeLog 复用 F026 前端组件 + /debug/a2a 接口 | +10-15 行 |
| **M5** | 21（line 2091）| 删"F026 接口未拍前实施 P5"风险行 | -1 行 |
| **M6** | 0 Changelog | 加 V16.5 条目 | +8-10 行 |

**总改动量**：~35-50 行 plan 文档增量，**0 改 skill / 0 改代码**。

---

## 8. 立项就绪度

| 维度 | 状态 |
|---|---|
| F026 落地代码深审 | ✅ |
| V16.4 vs F026 schema 冲突扫描 | ✅ 0 冲突 |
| V16.4 plan 过时引用清查 | ✅ 0 命中 |
| 6 处对接点定位 | ✅ |
| audit v3 C 类痛点 → F026 解决映射 | ✅ 全解 |
| 决策 4 闭环（F015 superseded / F026 done / F017 单独） | ✅ |
| 工时基线重算 | ✅ 58-78 单人天 |
| **skill 改动** | **❌ 按小孙约束不动** |

---

## 9. 待小孙拍板

### 决策 9：V16.5 修订是否落地（🔴 必拍）

- **A（推荐）**：落 6 处修订（V16.4 → V16.5），35-50 行文档增量，开发时直接消费 F026 落地物
- **B**：不落，开发到对接面时由 owner 自然消费（plan 不动，V16.4 保留）
- **C**：仅落 M5（删过时风险行）+ M6（Changelog），其他 4 处对接面留给开发判断

**我的建议**：A。理由 — 6 处 reference 注解防开发时 reinvent（章节 18 最危险，agent 不知道 /debug/a2a 已存在可能新建一份）。

### 决策 10：F015 状态如何标（🟢 低优）

F026 commit msg 明示 "Supersedes F015"。是否在 docs/features/F015*.md frontmatter 标 status: superseded + supersedes_by: F026？

- **A（推荐）**：标 superseded，链到 F026
- B：留 spec，待立项第一批后看是否还需要

### 决策 11：F017 何时 audit（🟢 低优）

F026 没碰 cross-room。F017 独立。

- **A（推荐）**：V16.5 第一批立项后做 F017 单独 audit（不挤本次）
- B：现在顺手 audit

---

## 10. v4 vs v3 关键差异

| 维度 | v3（V16.4 落地后）| v4（F026 合 dev 后）|
|---|---|---|
| F026 状态 | deferred | ✅ **done** |
| 决策 4 闭环 | ⏸ 暂缓 | ✅ **闭环**（F015 superseded / F017 N/A） |
| V16.4 plan 修订 | 无 | **6 处 V16.5 增量**（M1-M6）|
| 工时基线 | 63-83 单人天 | **58-78 单人天**（-5-8 天 F015 节省）|
| C 类 A2A 痛点 | V16.4 不解 | ✅ **F026 已全解** |
| skill 改动 | 暂缓 | **按小孙约束不动** |

---

**生成于**：2026-05-08，黄仁勋  
**触发**：F026 944b7b1 合 dev / 2026-05-08 16:16  
**深审依据**：944b7b1 commit msg + schema.ts diff + sqlite.ts SQL + 9 个新 runtime 模块代码 + V16.4-final.md 全文 grep + audit v3 决策状态对账  
**约束**：skill 不动（小孙 2026-05-08 拍板）
